# 同程公开航班与国际低价日历核验

## 国际航线

2026-09-14 实测同程国际站的公开低价日历：

- [同程国际机票首页](https://www.ly.com/iflight/)
- [上海→东京日历请求示例](https://www.ly.com/miflightapi/ts/calendar?D=SHA&A=TYO&DD=2026-10-01&AD=&TT=OW&ST=1)
- [同程国际站当前前端脚本](https://file.40017.cn/iflight/iflight/app.061a10b4a76a72d0efc4.js)

该日历使用匿名 HTTPS GET。请求中的 `pc-token: 1`、`t-token: 1` 是官网写死的渠道标记，不是用户凭证，也不需要配置 API token。实测请求不带 Cookie、账号、动态签名或登录会话即可返回 `code=200`、`data.R="0"` 以及约 90 个日期行。程序不执行同程 JavaScript，不处理验证码，也不调用受保护的实时列表接口。

官网前端的字段流向可以直接确认价格口径：航班列表中最低销售价 `sp` 写入日历 `P`，最低总价 `tp` 写入日历 `TP`；页面默认 `isTax=true`，低价排序也使用 `tp`。程序因此只采用日期行中的正数 `TP`，不会拿顶层全日历最低价、`P` 或其他日期填补用户所选日期。

一次请求可以覆盖响应中的所有所选日期，比逐日打开页面快。最终响应必须仍是同程官方 HTTPS 日历地址，唯一携带原请求的 `D/A/DD/AD/TT/ST` 参数并明确标为 JSON；每行必须满足精确的 `DD` 日期、空返程字段 `RD`、有效 `P` 和 `TP`，且 `TP >= P`。重定向到其他地址或路线、错误内容类型、重复日期、失败状态、字段变化、非标准数字、负数和非 JSON 验证页面都会使该来源失败。

这个接口仍有三个边界：它是未承诺长期兼容的官网内部日历；返回的是缓存参考价而非实时可售库存；响应没有回显出发地和目的地。程序将请求固定到同程官方 HTTPS 域名，在请求 URL 中携带用户选择的城市代码，并只匹配精确日期，同时在报价说明中公开“响应不回显航线”这一限制。缺失日期可能是没有缓存、没有航班或超出约 90 天范围，不能据此判断售罄。

## 国内航线

2026-09-08 使用匿名 HTTPS GET 实测，不带 Cookie、token 或登录会话。例如 [北京→上海，2026-09-22](https://www.ly.com/flights/itinerary/oneway/BJS-SHA?date=2026-09-22)。

页面内 `window.__NUXT__` 的 `state.book1.flightLists` 含初始航班数据。程序只接受有限的数据字面量语法，不执行网页 JavaScript；遇到额外表达式、验证页、重复字段、复杂度超限或结构变化会停止解析。页面和每条航班的城市、日期必须与用户选择一致。

价格口径依据同程自己的静态代码：

- [航班列表脚本](https://file.40017.cn/assets/flightpc/e9a826dca4d7610c2ac4.js)的单程价格显示使用 `lcp`。
- [结算脚本](https://file.40017.cn/assets/flightpc/aea8b2dd28cdc40b6ea5.js)将 `pt` 映射为成人机建、`ot` 映射为成人燃油，计算成人票价加两项费用。特殊 `g5flag=1` 且 `g5pt` 非零时按官网逻辑替代机建费用。

每条报价保留实际组成，不按统一税费估算。2026-09-22 出发的实测快照：

| 航线 | 航班 | 票价 | 机建 | 燃油 | 参考总价 |
| --- | --- | ---: | ---: | ---: | ---: |
| 北京→上海 | MU5138 | 450 | 50 | 70 | 570 |
| 上海→北京 | CA8322 | 490 | 50 | 70 | 610 |
| 上海→喀什 | G59526Y | 1130 | 50 | 140 | 1320 |

以上都是验证当时页面返回的参考金额，非当前可购保证。华夏联程的中转次数未确认，不显示成直飞。

该页面只返回**初始部分航班**，不是该平台全部库存；每日期输出这些航班中可确认含税总价的最低一条。缺少税费、多个航段费用不明等情况不参与比较。往返、指定舱位和直飞筛选暂不支持，并明确报告。国际航线使用上面独立的低价日历，不会把国内页面结果当作国际报价。

每日期一次请求，计入每轮平台预算；默认间隔 1 秒。取消监控后停止尚未发出的日期请求；已经在途的请求可能要等网络超时，但不再提交提醒。

另已核实同程公开低价日历接口 `/flights/api/getpricecalendar`，但其价格未提供可确认税费，因此没有用它冒充含税报价，也没有用来填补页面失败。
