# 平台核价入口与紧凑状态卡（2.5.0，2026-09-14）

平台状态卡的逐日说明改为原生折叠详情，默认只显示平台、查询状态、报价数、可比最低价和“打开平台核价”。同程与飞猪国际卡片的链接携带精确城市代码、城市名和最早出发日期；后台无法越过同程动态签名或飞猪滑块时继续明确显示限制，不将验证码页、营销起价或附近城市价格作为报价。

结果区另有默认收起的 6 个补充入口：Trip.com、Skyscanner、KAYAK、momondo、春秋航空、AirAsia，均携带同一航线和最早出发日期。它们因验证码或动态页面不能稳定无人值守取价，所以不会计入自动比价平台数。相关 URL 构造、日期/路线保留、后端结果暴露和 338 项自动测试已验证；Python 编译、JavaScript 语法、Shell 语法及真实 Chromium 页面检查通过。

# 免费微信服务号与扫码绑定验证（2.4.0，2026-09-10）

新增 Server酱 Turbo 方糖服务号通知，供 Linux 后台使用，无需桌面微信或 ClawBot 的周期互动。固定发送通道 9；按提供方当前免费额度，本程序每天最多提交 5 次，包括测试与失败，达到上限后停止提交、继续查价，北京时间次日恢复预算。没有订阅、付费、充值或绕开服务限额的操作。

页面提供“生成绑定二维码 → 手机微信扫码 → 我已扫码，完成绑定”的首次授权流程。程序自动保存 SendKey；授权仍存在，不能宣传成完全无凭证。新账号需按官网指引完成首次开通并关注方糖服务号，未完成时明确显示提示。备用手填 SendKey 收在折叠区域。绑定本身不发消息、不开始监控、不改行程。

真实只读验证与依据：

- 读取 Server酱 当前官网加载的公开前端，核实匿名 `GET /user/signin`、用户点击后的 `POST /sso/check` 及结果对象 `data.result.sendkey`；通道编号 9 来自官网 `channel_options_suggest`。
- 匿名二维码接口和微信官方图片下载实测成功，绑定模块返回 waiting、有有效 JPEG data URL、约 597 秒剩余。本地取消测试会话，未扫码、未注册、未发送微信。
- 实际响应的扫码目标是 `http://weixin.qq.com/q/...`，它仅作字符串校验，程序不访问该地址；图片地址和登录接口始终使用 HTTPS。已兼容图片真实 MIME `image/jpg` 并规范为 `image/jpeg`，保留文件签名检查、响应上限及证书验证。
- 自动审批拒绝了读取临时登录凭证并提交扫码状态检查的实测，理由是缺少该凭证出站的明确授权。未重试或绕过该操作；真实授权确认及微信收件仍需用户在页面主动扫码、点击确认及测试。匿名 GET 验证不调用该确认接口。

335 项自动测试通过，包含 25 项扫码绑定、24 项通知器及 15 项新增网页整合测试。验证了凭证格式、过期清理、地址限制、禁止重定向、TLS 验证、持久限额、跨线程/进程竞争、跨日恢复、用户操作互斥、失败不记为已提醒，以及原有七平台查价和通知回归。Python 编译、JavaScript 语法和命令行配置预览检查通过；未配置服务号时正常发送模式明确拒绝启动，未静默切换通知方式。

真实 Chromium 使用独立临时数据目录、模拟授权与发送传输完成页面验收：

- 新页面城市为空、七平台全选；选择服务号后未绑定不能开始监控，但可以查价。
- 生成二维码后本地状态轮询不会自动请求外站确认；只有点击确认才完成模拟授权。密钥不出现在 bootstrap/status/行程配置/localStorage/URL，保存文件权限为 600。
- 绑定后保持原城市、同一天出发日期和勾选平台；没有自动开始监控或发消息。
- 5 次模拟测试提交后，第六次按钮禁用，查询仍可用；模拟跨日后恢复 5 次，监控正常提交一条含航线、日期和价格的标题。
- 监控时禁止绑定、修改凭证或发送测试；提醒记录显示“已受理，请在微信确认”，不会标为送达或产生浏览器通知。
- 跨站保存和绑定请求被拒绝，私密文件不可经静态地址读取。桌面和 390 像素手机宽度无脚本错误或横向溢出，已人工查看截图。

主网页升级至 2.4.0，原 8765 地址保留；更新前无活动查询或监控，更新后没有自动启动监控，没有绑定用户账号。来源、首次准备与接口限制见 [SERVERCHAN.md](SERVERCHAN.md)。下面保留旧版本记录。

---

# 国内外七平台验证（2.3.0，2026-09-09）

在携程、同程、去哪儿基础上新增飞猪、Google Flights、Kiwi.com 和瑞安航空官网。七个来源均使用匿名公开查询，无需 API key、平台登录 token 或推送 token；运行时使用 Python 标准库，不要求安装浏览器。Chromium 仅用于本次网页验收。

新页面城市为空，默认七个平台全选，分为国内旅行平台、海外旅行平台、航空公司官网三组。此前保存的有效平台选择继续恢复，不会擅自扩大已保存规则。用户可以搜索其他城市、更换日期区间和勾选平台，各来源的覆盖限制与失败原因单独显示。

真实 Chromium 操作城市搜索、日期输入和平台选择，逐次等待后端查询完成，核对结果中的航线、日期、所选来源与本次请求一致，然后断言页面内容。使用独立临时数据目录，没有改变用户行程或发送微信。以下为验证当次快照，不能视为锁价或全网最低价保证：

| 航线与出发日期 | 实际返回 | 页面结果 |
| --- | --- | --- |
| 北京 → 上海，2026-09-23，国内四平台 | 携程 ¥520；同程 ¥610；飞猪 ¥530（票价 410 + 税费 120）；去哪儿未含税 ¥388 | 最低参考总价 ¥520，未含税价排除；飞猪报价及官网链接可见 |
| 上海 → 东京，2026-10-07～09，Google Flights + Kiwi | Google 分别 ¥902、¥926、¥902；Kiwi 分别 ¥967、¥1014、¥960 | 最低参考总价 ¥902；Kiwi 成人数和税费口径未确认，三条均只展示参考价 |
| 伦敦 → 巴黎，2026-10-06，Google Flights + 瑞安 | Google ¥269；瑞安未返回该日航线报价 | Google 成为本次最低，瑞安明确显示暂无报价 |
| 伦敦 → 都柏林，2026-09-29，仅瑞安 | GBP 14.99，折算 ¥136.26 | 显示原价与汇率说明；由于日历含税口径未确认，不显示最低总价，不触发阈值 |

瑞安上述折算使用 Frankfurter 2026-09-08 参考汇率 1 GBP = 9.0898 CNY。查询覆盖官网目录映射到伦敦的全部机场组合；最低来自 LGW。汇率必须日期有效、币种匹配且金额有效，不会用 1:1 替代失败的汇率。换汇结果仅供参考，不代表支付结算汇率。

验证结果：

- 271 项自动测试通过，覆盖新来源解析、身份匹配、税费和附条件票价过滤、请求预算、取消与失败隔离；最后的文案修正后，11 项瑞安测试与 JavaScript 语法检查再次通过。
- 默认命令行配置检查通过，显示七来源的基础请求估计和瑞安完整机场组合预算说明。
- 新来源购票链接按官网域名白名单显示。未知价不获得最低价行标记；只有未知价时隐藏最低总价和目标价格标记，仍保留来源状态与报价。
- 浏览器无页面脚本错误；390 像素手机宽度无横向溢出。已人工查看桌面和手机截图。
- 主服务已重启至 2.3.0；首页、`/api/bootstrap`、`/api/status` 均 HTTP 200，接口返回全部七来源。用户服务 active、enabled，Linger=yes。更新前确认无正在查询或运行的监控，更新后没有自动开启监控。

来源依据及覆盖限制见 [SOURCES_FLIGGY.md](SOURCES_FLIGGY.md)、[Google Flights 文档](docs/google-flights-source.md)、[KIWI_SOURCE.md](KIWI_SOURCE.md)、[OVERSEAS_SOURCES.md](OVERSEAS_SOURCES.md)。Skyscanner、KAYAK 等本次验证遇到访问验证/拦截的站点未列为已接入来源。

本次未发送真实微信消息。当前 Linux 环境没有桌面微信；免 token 微信发送仍需在已登录兼容微信的 Windows 电脑运行。此前的端口与版本记录保留如下。

---

# 端口恢复与后台服务验证（2026-09-09）

故障时宿主机 `127.0.0.1:8765` 返回 Connection refused，没有监听进程，用户服务尚未安装。此前的临时网页进程已退出。

已安装并启用用户服务 `flight-price-watcher.service`，工作目录为项目目录，使用实际 Python 绝对路径启动。修复安装脚本的 `WorkingDirectory` 引号导致 systemd 拒绝启动的问题，增加 `systemd-analyze --user verify`；网页导入检查不再依赖独立 CLI 配置。服务设置 `Restart=always`、`RestartSec=5`。

当前用户 `Linger=yes`，用户服务可在退出所有登录会话后继续运行。只监听本机，远程电脑仍需 SSH 或 VS Code 端口转发，具体步骤已补入 README。需要撤销驻留时可执行 `loginctl disable-linger "$USER"`（这会影响该用户其他后台服务的驻留）。

实际验证：服务 enabled、active/running；首页、`/api/bootstrap`、`/api/status` 全部 HTTP 200，返回版本 2.2.0 和携程/同程/去哪儿三个平台。没有自动启动机票监控，没有发送微信。未通过重启主机或退出全部会话做破坏性验证，驻留和自启动结论依据已生效的系统配置。

---

# 多平台比价验证（2.2.0，2026-09-08）

修复前默认只查询携程。现在网页默认选择携程、同程、去哪儿，并显示每个平台的真实查询状态、报价来源及税费口径。命令行 `config.toml` 示例也使用 `provider = "multi"`；可以缩小查询平台集合。

核心聚合器真实匿名查询同一国际航线的结果如下（查询当天的快照，不保证当前可售）：

| 上海 → 东京出发日 | 携程含税参考总价 | 去哪儿含税参考总价 | 本次最低来源 |
| --- | ---: | ---: | --- |
| 2026-09-29 | ¥1840 | ¥1748 | 去哪儿 |
| 2026-09-30 | ¥1962 | ¥2677 | 携程 |

真实 Chromium 联合验证：北京→上海 2026-09-22～23 三平台均返回两天报价，携程最低总价 440 元、同程最低总价 570 元；去哪儿 355/320 元是未含税价，页面正确排除总价排名。上海→东京 2026-09-29～30 两平台比价显示去哪儿最低 1748 元；第二次查询中携程 9 月 30 日价格已从 1962 变为 2702 元，说明以上记录是逐次快照，不能视为锁价。

同程仅对可确认机建、燃油的国内公开页面航班进行总价比较；国际显示暂不支持。去哪儿国内日历是未含税票面价，可显示但不参与最低总价或阈值判断，国际日历按官网标示的含税口径使用。来源依据见 [TONGCHENG_SOURCE.md](TONGCHENG_SOURCE.md)、[QUNAR_SOURCE.md](QUNAR_SOURCE.md) 与各 provider 模块。

187 项自动测试通过，包含 27 项同程解析/取消测试、17 项去哪儿解析/城市/取消测试及监控整合回归。已验证的平台边界：

- 各平台并行查询；一家网络/解析/初始化失败保留其他家的报价，并单列失败原因。
- 币种、出发地、目的地、日期和往返身份不匹配的报价被排除。
- 税前低价不会触发总价阈值；切换所选平台会建立新的提醒规则状态。
- 提醒同时列出各平台最低参考总价或失败原因，并报告可比平台数。
- 独立 Chromium 模拟数据边界测试：80 元未含税价不压过 500 元总价；第二平台 450 元总价正确成为最低；只有不可比价时隐藏最低总价和目标标记；空平台选择被拒绝；恶意相似域名购票链接不显示。
- 手机 390 像素页面无横向溢出，表格在自身区域滚动，无页面脚本错误。
- 数据库锁冲突或无法打开时保留已经查询成功的报价和各平台状态；停止监控会取消后续城市/日期请求，已在途请求结束后不提交提醒。

主服务已升级至 2.2.0，初始城市为空，默认三平台全选。没有自动开启监控，没有发送真实微信消息。下面保留旧版本的独立验证记录。

最终命令行以独立临时配置运行 `--dry-run`，国内/国际同轮分别预览最低价汇总 440 元和严格低于 2000 元的提醒 1748 元，均包含三个平台的价格或限制原因。由于同程覆盖和去哪儿国内税费限制，退出码为表示部分结果的 `1`；不是整轮查询失败。预览使用内存数据库，没有写入真实历史或发送微信。

---

# 地点自由搜索验证（2.1.0，2026-09-08）

修复前只提供 53 个常用城市候选并预填上海→北京。现在新页面的出发地、目的地为空，输入中文、拼音或代码后搜索携程收录的城市。原名单仅保留为快捷候选，不再限制查询范围。

116 项自动测试通过，包含动态城市服务、截断响应错误处理和原有监控功能。真实 Chromium 浏览器验证通过：

- `kashi` 搜索并选择喀什 KHG，上海→喀什 2026-09-22～2026-09-24 当次最低日历参考总价 1220 元。
- 中文“布拉格”选择 PRG，自动识别国际航线，相同日期窗口当次最低日历参考总价 3514 元。
- 清空目的地后可重新选择；方向键和 Enter 成功选择阿勒泰 AAT。
- 手机宽度下城市下拉框可显示，无横向溢出，无页面脚本错误。
- 验证使用独立临时数据目录，没有改变用户保存的行程，也没有发送微信。

价格仅是验证当时返回的参考总价。城市服务故障会明确提示，并显示已有匹配候选，不会将失败伪装为“没有这个城市”。

---

# 免 token 网页版验证（2026-09-08）

当前版本 2.0.0。新网页和默认本机微信发送不要求 token；不再用 PushPlus 作为默认通知方式。

- 97 项自动测试通过：含城市/日期校验、国内国际价格解析、阈值、持久去重、微信客户端返回失败、停止监控取消通知、任务串行占用及快速切换查询。
- 使用真实 Chromium 浏览器验证：选择上海→北京、上海→东京，选择 2026-09-22～2026-09-24，分别返回 3 天有效日历价格。当时区间最低参考总价为北京 540 元、东京 1681 元；不是当前可购报价保证。
- 浏览器提醒已在独立临时数据目录启动、触发一条提醒、停止成功；没有发送微信，没有覆盖用户的原有配置或数据库。
- 确认页面无 token/key 输入字段；桌面微信不可用时明确显示状态，不静默替换通知渠道。
- 浏览器无页面脚本错误；390 像素手机宽度没有横向溢出。界面截图已人工查看。
- 本地接口拒绝跨站操作请求，`.env` 不可通过静态路径访问。
- Python 编译、JavaScript 语法、Linux 启动脚本语法检查通过。

真实微信发送尚未完成实机验收：当前机器是无桌面微信的 Linux。Windows 桌面组件锁定 wxauto4 41.1.7，上游说明对应微信 4.1.8；已审计其接口声明并做模拟测试。需要在已登录兼容微信的 Windows 电脑运行并测试文件传输助手，不能将模拟测试视为手机已收到消息。

主页面已启动于本机 `http://127.0.0.1:8765`。没有默认开始微信监控；由用户在页面选择行程和通知方式后点击开始。

---

# 上一版本验证记录（2026-09-08 16:30）

验证环境：Python 3.12.7，2026-09-08，Asia/Shanghai。

## 真实查询

执行 `python3 watch.py --once --dry-run`，使用默认 `config.toml`，查询 2026-09-15～2026-09-29。2026-09-08 16:30 完成，退出码 0，四条航线各返回 15 条有效日历价格。

| 航线 | 最低参考总价（CNY） | 对应出发日期 |
| --- | ---: | --- |
| 上海 → 北京 | 520 | 2026-09-16 |
| 上海 → 广州 | 369 | 2026-09-15 |
| 上海 → 东京 | 991 | 2026-09-16 |
| 上海 → 新加坡 | 711 | 2026-09-15 |

来源是携程移动低价日历的 `totalPrice`。这是当时返回的单程日历参考价，并非当前可购报价或全网最低价保证。查询只读，未发送微信，未写入真实历史数据库。

本次运行环境的受限网络沙箱无法访问外网，获准的只读网络执行成功；脚本无需关闭 TLS 验证或提供携程登录凭证。

## 本地检查

- `python3 -m unittest discover -s tests -v`：62 项测试全部通过。
- `python3 -m compileall -q flightwatch watch.py`：通过。
- `bash -n deploy/install-service.sh`：通过。
- `python3 watch.py --demo`：离线示例格式正常，明确标注模拟价格。

## 尚需个人账户配置的部分

`.env` 已创建，权限为 600，`PUSHPLUS_TOKEN` 为空。微信 API 使用模拟测试验证，尚未验证实际接收。没有启动常驻进程，也没有安装 systemd/cron 任务。配置好微信接收 token 后，可按 README 测试并启动。

备用 SerpApi 已实现并通过模拟响应测试；本次没有 API key，未进行其真实查询。
