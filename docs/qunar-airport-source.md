# 去哪儿机场查询

2026-09-14 检查。选择某个机场时已改为请求逐航班列表；两端均选“全部机场”仍使用原来的快速城市日历。

## 国际与港澳台

官网当前 [国际单程页面脚本](https://q.qunarzz.com/flight_qzz/prd/inter_oneway@6db14243cbf78cdedebe.js) 的 `SEARCH` 指向 `https://flight.qunar.com/touch/api/inter/wwwsearch`。筛选器把 `depAirport`、`arrAirport` 原样传入列表请求。程序使用该接口，请求所属城市、确切出发日期、1 成人、0 儿童，仅在用户指定的一端加入机场筛选。

响应必须确认成功、回显对应的出发与到达城市、轮询标识一致且 `ctrlInfo.completed=true`，然后逐条检查 `journeyType=ONEWAY`、单个 trip、首段起飞日期、首段出发机场和末段到达机场。城市日历和页面上的邻近推荐均不参与机场结果。

只在 `currencyCode=CNY` 且 `totalTaxType` 为 `GeneralTax=1` 或 `ContainTax=2` 时，将 `lowTotalPrice` 作为含税参考总价；官网枚举 `NoTax=0`、`ConsultTax=3` 及未知类型仅作参考。不对过期、错误航线、登录要求或验证响应生成报价。

每日期最多轮询 4 次，增量价格按航班身份更新，等待完成后才发布。首次响应失败就停止剩余日期，避免同一验证或错误缓存造成几十次无用请求。预算上限为 2 次所属城市识别 + 4 × 日期数；成功城市识别会缓存在 provider 中。

## 国内

官网 [国内单程页面脚本](https://q.qunarzz.com/flight_qzz/prd/domestic_oneway_new@1e3350c8ebf823f5308f.js) 使用 `POST https://flight.qunar.com/touch/api/domestic/wbdflightlist`，参数是 `departureCity`、`arrivalCity`、`departureDate`。程序读取其 `flights`，仅接受已确定航班的 `list` 和 `listMore`，核验首末段所属城市、日期和实际机场。

国内列表中的机场名称通过官网 [地点建议接口](https://m.flight.qunar.com/touch/api/suggest?queryWord=SHA) 的同城市包含机场进行唯一映射；不通过“上海”推断浦东，也不使用邻近城市机场。`minPrice` 没有明确含税证明，所以国内仍是票面参考价，不用于总价比较或微信阈值提醒。预算为 2 次城市识别 + 每日期 1 次列表请求。

## 实际验证结果与限制

本次匿名实测 `PVG → CJU`、2026-09-21 国际请求获得 HTTP 200，但官网接口回显“北京 → 名古屋”、`completed=false`，并无可用报价；程序将其报告为错航线或过期缓存。无缓存请求参数未改变该结果。

国内 `PVG → PEK` 同日返回 `ret=true`、`code=-1`、空 `flights`、空 `geographyInfo`，没有可验证的路线或日期，程序报告查询失败，不显示为“没有航班”。两种实测均使用 3 次请求即停止后续日期。

因此本次交付是机场查询请求、逐航班验证与错误处理接入，**不等于当前网络已成功获得去哪儿机场报价**。新增测试根据官网公开字段建立契约测试，并覆盖上述真实失败形态；测试中的示例票价不是实测票价。

购票入口包含原路线、确切日期、成人数及官网的 `filterFlightCode` 参数。官网可能重新搜索或调整展示，出票前仍须核对实际机场、航班与最终售价。

## 官方 API 申请核查

2026-09-14 再次核对公开官方资料，未找到供个人开发者自助领取密钥、明确免费额度的国际机票查价 API。这里是“未找到官方承诺”，不能推断商务接入一定收费，也不能承诺免费。

| 所需信息 | 官方公开证据与结论 |
| --- | --- |
| 正式申请入口 | [去哪儿业务合作页](https://www.qunar.com/site/zh/Cooperate_4.shtml) 单列“分销机票合作” `Open@qunar.com`；国际机票合作为 `gjkhfw@qunar.com`。只记录联系方式，本项目没有代发申请。 |
| 面向个人吗 | 同页“机票合作－点击加入”通往 [商家入口](https://adreport.qunar.com/)，实际转至 `business-settle/login`。其[公开入驻表单脚本](https://q.qunarzz.com/R_nodefe-qss_business_settle_qzz/prd/p__settleFlow__index.ddbb2007.async.js)要求公司名称、营业执照号、法人等，并有业务合作与资质审核。未看到面向个人开发者的独立查价 API 申请表。 |
| 费用和额度 | 上述公开合作页没有公布查价 API 的免费额度、每次费用或个人免费套餐。商家表单包含首次付款与保证金字段，但那是商户业务合作表单，不能直接当成分销查价 API 的费用说明。 |
| 含税价和具体机场 | 官网消费者列表具备这些字段，已有严格解析；正式分销 API 是否授权这些能力，公开申请页未给出对应接口文档与权限承诺，需要合作方确认实际获批能力。不能把消费者网页接口当作已获授权的合作 API。 |

另须区分：[官网国际酒店 Open 平台](https://open.hotel.qunar.com/) 明确是供应商酒店接入，其[标准文档](https://open.hotel.qunar.com/doc/api-cn.htm)描述代理商提供酒店和报价、去哪儿调用供应商接口；它不是让本项目查询去哪儿国际机票最低价的 API。[商家登录页的公开协议脚本](https://q.qunarzz.com/R_nodefe-qss_business_settle_qzz/prd/p__login__index.6ae4f124.async.js)虽提到“个人代理商”和 API 密钥，其平台定义是代理商向去哪儿传递产品、服务及报价信息，也不能据此宣称个人可免费查询所有机票。

## 正常官网浏览器对照

为排除 HTTP 方法、遗漏网站正常会话或机场参数导致旧回显，再进行了一次无登录的官网普通浏览器访问，使用上海 → 济州岛、2026-09-21、1 成人、0 儿童。

官网自身脚本实际发送 `GET /touch/api/inter/wwwsearch`，参数 `depCity=上海`、`arrCity=济州岛`、`depDate=2026-09-21`、`adultNum=1`、`childNum=0`，没有机场过滤参数。其自动请求中已读取的三次响应均为 HTTP 200，但 `ctrlInfo` 回显北京 → 名古屋，`completed=false`、0 航班，与现有 provider 的异常一致。浏览器最终页面进程崩溃，未取得有效页面报价。

这个对照说明问题同样发生在本站浏览器正常请求，不能通过将 GET 改成 POST 或去掉机场筛选来解决；仍不能确认问题发生在网站源站、缓存或当前访问链路的哪一层。未修改解析器放宽身份校验，也没有继续重复请求或处理登录/验证码。

## 免费公开路径追加核验

2026-09-14 进一步检查移动端 `https://m.flight.qunar.com/ncs/page/interlist`。普通无账号浏览器的官网代码实际调用 `POST /flight/api/touchInterList`，所选城市、日期及 1 成人条件正确。页面自然产生的查询连续返回 `bstatus.code=1999`、`code=-1`、`ret=false`、`data=null`。即使文本 `msg` 写着“成功”，也没有航班明细；不能视为有效报价。没有自行生成、修改或复用浏览器的安全签名。

公开城市日历则确实取得了部分当前日期报价：上海 → 济州岛，2026-10-04 为 955 元、10-05 为 655 元，均回显航班号 `9C8573`，已用现有日历解析器验证。这是免费取得的城市范围含税参考价格，不是机场筛选成功；该响应不含实际机场字段，不能将其包装为所选机场报价。本次 9/21–10/05 区间其余 13 天的日历价格为空，不代表没有航班。

另检查[公开航线特价页](https://flight.qunar.com/touch/international-routes-SHA-CJU.html)，其便宜的 88 元等链接携带的实际日期是 **2019 年**，不是 2026 年报价；没有纳入监控。上述新路径没有解决具体机场明细的访问问题，因此未给生产查询增加必然失败的移动端重试，也未放宽原有机场校验。

## 2.10.2：城市参考单独展示

新增免费日历备用查询：国际机场列表失败后，只额外请求一次日历，覆盖所选日期；不重复尝试已经失败的逐日列表。城市身份、日期、市场和价格仍由原有严格解析器校验。程序停止时不请求备用来源，正常机场查询成功时也不增加日历请求。

`SearchResult.city_references` 保存这些城市报价，与用于比价和提醒的 `quotes` 完全分开；机场字段保持为空，并强制不可比。聚合层再次核对所属城市和日期，将其放入平台报告 `city_reference_quotes`，不送入价格记录或通知引擎。前端显示“仅城市参考”和“机场未确认，不参与比价或提醒”，明细默认折叠，各日期链接携带原城市、日期及航班号查询提示；平台可能重新搜索，链接不保证直接进入付款页。

本次本机实测上海（全部机场）→ 济州（指定 CJU），2026-09-21 至 10-05，10.77 秒取得 10-04 的 955 元、10-05 的 655 元城市参考，均为 9C8573；13 天没有日历缓存价。实际机场匹配报价仍为 0。真实日历响应精简保存在 `tests/fixtures/qunar_city_reference.json`，仅用于解析回归，不作为在线报价缓存。

另外试查了官网移动端航班详情页面，其正常脚本请求 `/flight/api/touchInterOta` 仍返回 `bstatus.code=1999`、`ret=false`、`data=null`，页面显示加载失败；官网历史低价入口 `/api/lp_multifetch` 返回 `ret=false`。这些结果均不能证明指定机场价格，未作为成功来源接入。
