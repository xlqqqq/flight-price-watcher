# 去哪儿公开日历来源核验

2026-09-08 通过正常网页和不带 Cookie、token 的 HTTPS GET 请求核验；这些是网页后台的非公开承诺接口，网站改版或防护可能使它们失效。

- [去哪儿官方机票首页](https://flight.qunar.com/site/)实际加载的 [home_pack 脚本](https://q.qunarzz.com/flight/prd/scripts/home_pack@8bb0d634c8d64977d89e75708951029d.js)调用 `https://gw.flight.qunar.com/api/f/priceCalendar`，参数为 `dep`、`arr`、`days`（空字符串）、`priceType`（国内 1，国际 2）。代码读取 `data.gflights[].date/price`。返回的 `scity`、`ecity`、`flightType` 必须与查询匹配。
- 同一官方脚本用城市 `isInter` 设置日历的 `hasTax`，相应 UI 分支明确显示“未含税”与“含税总价”。所以国内日历价格按 `base` 展示，不能参与总价排名及阈值提醒；国际/港澳台按含税日历参考总价 `total` 展示。没有给国内报价擅加固定税费。
- 官方脚本还调用 `https://m.flight.qunar.com/touch/api/suggest?queryWord=...`。程序使用该接口验证城市身份。实测 `BJS` 建议里北京的去哪儿代码为 `PEK`，`SIA` 的西安为 `XIY`；两者前面还可能有无关境外城市。只有与已选代码一致的城市，或与已有城市元数据名称、国家一致的城市才可选中，排除国家、机场、景点及“包含”条目，不取首项。

实际匿名查询及网页交叉核对：

| 航线 | 出发日 | 日历价格 | 航班字段 | 价格口径 |
| --- | --- | --- | --- | --- |
| 上海 → 北京 | 2026-10-15 | ¥414 | MU5185 | 未含税票面价 |
| 上海 → 东京 | 2026-09-29 | ¥1748 | BR721/BR196 | 含税日历参考总价 |
| 上海 → 东京 | 2026-09-30 | ¥2677 | 9C6217 | 含税日历参考总价 |

同一时刻国内接口 365 天中仅 53 天有价、东京仅 8 天有价。空价格代表缺少有效日历缓存，不能判断没有航班或售罄。上述数字仅记录核验时结果，会随实时市场和缓存更新变化。

布拉格通过去哪儿自身建议接口 `queryWord=PRG` 唯一识别为 `PRG / 布拉格 / 捷克`；已选城市元数据即使显示“布拉格(捷克)”也不直接把括号名称当查询目的地。

每条首次查询最坏 3 次公开请求：两个城市建议加一次日历。同一个 provider 实例缓存已验证城市 24 小时，最多 512 城市；所有实际请求（包括失败请求）均计入 `requests_used/max_requests`，默认间隔 1 秒。只有所选出发日期被保存为报价；不支持的往返、直飞和指定舱位会明确报告。

春秋航空官网在普通浏览器访问中要求滑块验证，因此没有接入为数据源，也没有尝试绕过。去哪儿航班实时列表与其公开日历不同，本实现只读取已验证的匿名日历接口。
